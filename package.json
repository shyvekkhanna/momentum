{
  "name": "momentum",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @momentum/web",
    "build": "npm run build -w @momentum/web",
    "start": "npm run start -w @momentum/web",
    "lint": "npm run lint -w @momentum/web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
